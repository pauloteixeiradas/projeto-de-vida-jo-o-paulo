const botoes = Documentos.queryselectorAII(".botao");

for(let i=0;i <botoes.length;i++);
botoes[i].onclick=fungtion(){

    for(let j=0;j=0;j<botoes.lemgth;j++){
        botoes[j].classlist.remove("ativo")
    }

    botoes[i].classlist.remove("ativo");
